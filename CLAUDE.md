# 星読みアプリ

## 概要
ブラウザで動く占星術チャートアプリ（シングルHTMLファイル）。
生年月日・出生時刻・出生地を入力してネイタルチャート＋トランジットを表示。

## 現在のバージョン
**v14**（2026-09-10 更新済み）

## 主な機能
- ネイタルチャート（SVG描画）
- トランジットアスペクト（日付変更対応）
- 天体配置・ハウスカスプ・アスペクト一覧テーブル
- CSV保存 / チャート画像保存
- **個人プロフィール保存（localStorage）** ← v7で追加
- **年間トランジット表（エフェメリス）** ← v11で追加。年/月（または全年）・ハウス基準地（国別選択 or カスタム緯度経度）を指定し、日ごとの天体位置・ハウス・順行/逆行を一覧表示。CSV保存・スプシ貼付用コピー対応。「自分のネイタルハウスを使う」トグル（v12.1）で、基準地の再計算ハウスではなく自分のネイタルハウス（CD.houses固定）でH列を判定可能
- **サイン移動（イングレス）時刻の表示** ← v12で追加。同日中にサインが変わる日は移動先サインと正確な時刻を金色で併記
- **長期トランジット検証** ← v13で追加。起点日から1年/3年/6年/12年を選び、対象天体（火星・木星・土星・天王星・海王星・冥王星）について、ネイタルチャート基準の「サイン滞在期間」「ハウス滞在期間（ネイタルハウス固定）」「逆行期間」「ネイタル10天体との正確なアスペクト成立日（オーブ0°の瞬間、逆行によるスリーパスも含む）」を自動検出し、全体タイムライン＋天体別詳細で表示。CSV保存対応
- **天空イベントカレンダー** ← v14で追加。ネイタルチャートに依存しない「惑星同士」のアスペクト・サイン移動・逆行/順行・新月満月・日食月食・ドラゴンヘッド/テイルのサイン移動・複合アスペクト（グランドトライン/Tスクエア/グランドクロス/ステリウム）を期間指定で検出し、カレンダー/リスト表示・重要度・検証記録（localStorage）・同一アスペクトの過去/未来検索を提供。詳細は下記「天空イベントカレンダー」節を参照

## ファイル構成
```
astrology-app/
  index.html                 ← 画面構造・既存機能のロジック（HTML/CSS/JS）、天空イベントの新規<script src>読み込み
  sky-events.js               ← 天空イベントカレンダー：計算エンジン（アスペクト/サイン移動/逆行/月相/食/ノード/複合アスペクト検出）
  calendar-ui.js               ← 天空イベントカレンダー：UI（カレンダー/リスト表示、フィルター、詳細モーダル、天空図、検索）
  journal-store.js             ← 天空イベントの検証記録（localStorage CRUD）
  economic-events-data.js      ← 経済イベント（FOMC/CPI/PPI等）の手動データ。SkyEvents形式への変換ヘルパーも含む
```
新規4ファイルはビルド不要の`<script src>`読み込み（既存の単一HTML方針を維持しつつ、天空イベント機能のみロジック量が多いため分割）。読み込み順は必ず astronomy-engine CDN → index.html本体の`<script>` → sky-events.js → journal-store.js → economic-events-data.js → calendar-ui.js（index.htmlのグローバル関数・定数を後続ファイルが参照するため）。

## 天空イベントカレンダー（v14）
- **対象**：ネイタルチャートを問わない「今この瞬間の天体配置」の検出。既存のトランジット系タブ（ネイタルとの比較）とは独立した機能
- **検出イベント種別**：①惑星同士の5大アスペクト成立の正確な瞬間（二分探索、オーブ設定は歯車ボタンから変更可・初期値はコンジャンクション/オポジション/トライン8°・スクエア7°・セクスタイル6°）②サイン移動（イングレス、既存`findIngressJD`を再利用）③逆行/順行開始（既存`findStationJD`を再利用）④新月/上弦/満月/下弦（astronomy-engine組み込み`SearchMoonQuarter`）⑤日食/月食（astronomy-engine組み込み`SearchGlobalSolarEclipse`/`SearchLunarEclipse`、種別・極大時刻付き）⑥ドラゴンヘッド/テイルのサイン移動⑦複合アスペクト（グランドトライン/Tスクエア/グランドクロス/ステリウム。ヨッド・カイトは未実装のTODO）
- **【重要】ノードの実装方式**：`Astronomy.SearchMoonNode`は「月が黄道面を横切る瞬間」（約27日周期）を返す関数であり、「ドラゴンヘッドが何座にいるか」とは別物。そのためノードのサイン移動検出は独自にMeeusの平均ノード（Mean Node）近似式`calcMeanNodeLon(jd)`を実装し、惑星と同じ二分探索パターンでサイン境界を検出している。真交点（True Node）ではなく平均交点のため、市販の占星術ソフトが示す「ドラゴンヘッドのサイン移動日」と数週間ずれる場合がある（既知の仕様）
- **重要度判定**：外惑星同士（天王星・海王星・冥王星）のアスペクト＝🔥非常に重要、木星・土星と外惑星のアスペクト＝⭐重要、食のうち皆既/金環＝🔥・部分/半影＝⭐、複合アスペクトは基本⭐（全天体が外惑星の場合🔥）。内惑星⇄外惑星のアスペクトは頻度が高いためデフォルト「通常」（オーブに関わらず一律で「重要」に昇格させるとMoon-Pluto等の頻出アスペクトでノイズになるため、意図的に対象外にしている）
- **複合アスペクトの対象天体**：太陽・月・水星・金星を含めると成立/解消が頻繁すぎるため、天体フィルターの選択に関わらず火星〜冥王星のみを対象に固定（既存の長期トランジット検証タブと同じ設計判断）
- **天空図**：`drawSkyWheel()`（calendar-ui.js）は既存`drawChart()`の極座標描画パターンを踏襲した、ハウスを持たない1リングのサインホイール。イベント詳細モーダルで選択日時のスナップショットを表示
- **検証記録**：`Journal`（journal-store.js）が`hoshiyomi_event_journal_v1`キーでイベントID単位の事前メモ・実際の出来事・市場データ（日経/NASDAQ/VIX/DXY/米10年債/BTC/原油）・検証メモを保存
- **経済イベント**：`ECONOMIC_EVENTS`（economic-events-data.js）は手動管理の配列。現状はCPI/PPI/FOMCのサンプル数件のみ投入済み（2026-09-10時点の報道ベース、要更新）。将来的な自動取得APIへの差し替えを見据えたフラットなスキーマ

## TODOリスト
- [ ] プロフィール切り替え時にチャートを自動再描画
- [ ] モバイル表示の最適化
- [ ] 複数人の比較（シナストリー）表示
- [ ] 天空イベントカレンダー：ヨッド・カイトの自動判定を追加
- [ ] 天空イベントカレンダー：ECONOMIC_EVENTSの継続的な更新（または自動取得APIへの差し替え）
- [ ] 天空イベントカレンダー：日食のローカル可視性判定（`SearchLocalSolarEclipse`、要緯度経度）

## 修正履歴
- 2026-09-10: 「天空イベントカレンダー」を新規タブとして追加（v14）。詳細は上記「天空イベントカレンダー」節を参照。実装過程で2つのバグを検出・修正済み：①`Astronomy.SearchMoonNode`の勘違い（月の黄道面横断であってノードのサイン移動ではない）→独自のMean Node近似式に切り替え、②`EclipseKind`が小文字文字列enum（'total'等）であることを見落とし、大文字キーで判定していたため食の種別・重要度・タイトルが常にフォールバック表示になっていたバグを修正。ブラウザでの実地テスト（claude-in-chrome、ローカルHTTPサーバー経由）で新規タブの全機能（リスト/カレンダー表示・詳細モーダル・天空図・検証記録保存/再読込・オーブ設定・アスペクト検索）と既存タブ（長期トランジット検証）の非回帰を確認済み
- 2026-09-02: 「長期トランジット検証」タブを追加（v13）。起点日＋期間（1/3/6/12年）＋対象天体（デフォルト木星〜冥王星の5天体、火星は任意）を選ぶと、buildLongTermReport()が各天体を種別ごとの間隔でサンプリングし、①サイン境界通過（既存findIngressJDを再利用）②ネイタルハウス境界通過（findHouseCrossingJDを新規追加）③逆行/順行の切り替わり（findStationJD、微小オフセットでの速度符号反転を二分探索）④ネイタル10天体との5大アスペクト（コンジャンクション/セクスタイル/スクエア/トライン/オポジション）の正確な成立瞬間（findAspectExactJD）——をそれぞれ二分探索で検出。イベントを結合した全体タイムラインと、天体ごとの滞在期間・逆行期間・アスペクト一覧を`<details>`で表示。太陽・水星・金星・月は変化が速すぎ長期検証に不向きなため選択肢から除外（既存のトランジット一覧・月間ラインで代替）。astronomy-engineを使った実測（1990年出生のダミーネイタルで2020〜2032年を検証）で、木星の水瓶座入り(2020-12-19)・天王星の双子座入り(2025-07-07)・冥王星の水瓶座入り(2023-03-23)など既知の実際の移動日と一致することを確認済み
- 2026-07-12: 年間トランジット表に「自分のネイタルハウスを使う」トグルを追加。従来のH列は基準地（東京など）で毎日0:00に新規に作り直した「独立した参考チャート」のハウスであり、ネイタルチャートとは無関係だった（仕様通りだが紛らわしい）。トグルONで`buildEphemerisRows`の第5引数に`CD.houses`を渡し、日々のハウス再計算をスキップして固定のネイタルハウスで判定するように変更
- 2026-07-12: 【重要】トランジット天体の「第◯ハウス」表示バグを修正。renderPlanets()とCSV出力(dlPlanetsBtn)が、トランジット天体のハウス判定に`getTransitHouses()`（今この瞬間を東京基準で作り直した別のハウス系）を使っており、ネイタルのASC基準で描かれる二重チャート図と数字が矛盾していた（例：図では自然な位置なのに表示ハウス番号が合わない）。`planetHouse(tp[p],CD.houses)`でネイタルハウス基準に統一し、`getTransitHouses()`は削除。表示ラベルも「東京基準」→「ネイタルハウス基準」に変更。テスト出生データで図とハウス番号が一致することを検証済み
- 2026-07-12: 年間トランジット表に「サイン移動（イングレス）時刻」表示を追加。0:00時点のスナップショットだけでは同日中にサインが変わる日（特に月は2〜3日に1回発生）が前のサインのまま表示され誤解を招くため、findIngressJD()（2分探索）で当日→翌日0:00の間の正確な境界通過時刻を検出し、セルに「→移動先サイン HH:MM」を金色で追記。CSV/コピー出力にも移動時刻列を追加。astronomy-engineによる独立検証で計算自体は元々正確だったことを確認済み（バグではなく日次スナップショットの仕様上の見た目のズレ）
- 2026-07-11: 年間トランジット表タブを追加（buildEphemerisRows/renderEphemerisTable等）。順行/逆行はcircDiff()で前日比の黄経差分から判定（太陽・月は常に'-'）。ハウス基準地はCITIESから選択、またはカスタム緯度経度。CSV/TSVコピー出力対応。あわせて`<!DOCTYPE html>`/`<head><meta charset="UTF-8"></head>`が欠落しておりcharset未指定サーバーで日本語が文字化けする問題を修正
- 2026-05-07: 天体計算をastronomy-engine v2（VSOP87ベース）に完全移行。calcPlanet()をGeoVector+Ecliptic+EclipticGeoMoonに差し替え。光行差・章動・歳差補正を含む精度（誤差1分角以内）。CDN: astronomy-engine@2/astronomy.browser.min.js
- 2026-05-06: 天王星補正係数バグ修正（0.832→5.31、e≈0.046に正しく対応）。ハウス計算をPorphyryから本物のPlacidus（semi-arc三分割・Newton-Raphson反復）に修正。緯度・経度直接入力フィールドを追加（精度優先時に使用）。
- 2026-05-04: チャート方向バグ修正（時計回り→反時計回り）、ハウス番号ズレ解消、全惑星を地心座標変換に修正（水星・金星のサインズレが主因）
- 2026-05-04: チャート配置を標準西洋式に変更（ASC左・MC上・DSC右・IC下）。角度オフセット -90° → +180°

## 技術メモ
- 天体計算：astronomy-engine v2（VSOP87ベース、誤差1分角以内）CDN読み込み
- ハウス・アスペクト計算：Pure JS実装
- ハウスシステム：プラシダス
- デザイン：ネイビー×ゴールドのクラシック占星術スタイル
